# Core pipeline (no audio / TensorFlow)
questionary>=2.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
requests>=2.28
PyYAML>=6.0
python-dotenv>=1.0
tqdm>=4.65

# discogs-xml2db (also install vendor deps after submodule init)
docopt>=0.6.2

# Optional: audio embeddings — install with:
#   pip install -r requirements-audio.txt
